Third-party licence notices for the Gauge workspace.

This file is generated by `cargo about generate about.hbs` and lists every crate
linked into the Gauge binaries together with its licence.

Licence summary (by SPDX licence, crate count):

{{#each overview}}
  - {{{name}}}: {{count}} crate{{#unless (eq count 1)}}s{{/unless}}
{{/each}}

Full notices follow, one block per licence, listing the crates it covers.

{{#each licenses}}
--------------------------------------------------------------------------------
{{{name}}}
  Used by: {{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}
--------------------------------------------------------------------------------

{{{text}}}

{{/each}}
